image: golang:1.11

pipelines:
  branches:
    master:
      - step:
          script:
            - source bitbucket-pipelines.sh

            - go get -t -v -u ${PKG_PATH}/...
            - go test -v -cover ${PKG_PATH}/...
